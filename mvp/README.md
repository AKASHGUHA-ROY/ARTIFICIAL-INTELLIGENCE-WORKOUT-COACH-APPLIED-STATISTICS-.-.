# AI Workout Assistant MVP

## Overview
This MVP is a Streamlit-based workout assistant. The user enters workout context such as target muscle group, available equipment, workout duration, experience level, effort level, pain status, and notes. The app returns a safe personalized next-step recommendation.

## Setup

Install dependencies:

```bash
pip install -r mvp/requirements.txt
streamlit run mvp/app.py is used to run the demo in GITHUB'S code spaces .
